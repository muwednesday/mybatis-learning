<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.muwednesday.dao.UserMapper">

    <insert id="insertUser" parameterType="User">
        INSERT INTO USER(username, password, date, sex, underline_test)
        VALUES (#{username}, #{password}, #{date}, #{sex}, #{underlineTest})
    </insert>

</mapper>